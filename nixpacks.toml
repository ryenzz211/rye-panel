[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'Build completed'"]

[phases.start]
cmds = ["node backend/server.js"]

[build.env]
NODE_ENV = "production"
PORT = "3000"
